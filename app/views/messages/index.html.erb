<h1>Messages</h1>

<%= link_to "Add a Message!", new_message_path %>

<% @messages.each do |message| %>
  <div>
    <h4><%= message.body %></h4>
    <small><%= message.username %></small>
    <small>Created <%= time_ago_in_words(message.created_at) %> ago</small>
  </div>
<% end %>
